import * as S from './inputErrorMessageStyles';

function InputErrorMessage({ children }: { children: React.ReactNode }) {
  return <S.InputErrorMessage>{children}</S.InputErrorMessage>;
}

export default InputErrorMessage;
